% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/riemannian_geometry.R
\name{frechet_mean_spd}
\alias{frechet_mean_spd}
\title{Fréchet Mean of SPD Matrices}
\usage{
frechet_mean_spd(
  S_list,
  metric = c("logeuclidean", "airm"),
  regularize_epsilon = 1e-06,
  max_iter = 50,
  tol = 1e-05,
  step_size = 0.5,
  init_method = c("euclidean", "first"),
  verbose = FALSE
)
}
\arguments{
\item{S_list}{A list of SPD matrices.}

\item{metric}{Character string, either `"logeuclidean"` (default) or `"airm"`.}

\item{regularize_epsilon}{Epsilon for regularization. Default 1e-6.}

\item{max_iter}{Maximum number of iterations for Log-Euclidean algorithm. Default 50.}

\item{tol}{Tolerance for convergence for Log-Euclidean algorithm (Frobenius norm
of the mean tangent vector). Default 1e-5.}

\item{step_size}{Step size for the gradient descent in Log-Euclidean. Default 0.5.}

\item{init_method}{For Log-Euclidean, method to initialize the mean: 
`"euclidean"` (Euclidean mean of matrices) or `"first"` (first matrix in list).
Default `"euclidean"`.}

\item{verbose}{Logical, if TRUE, prints iteration info for Log-Euclidean. Default FALSE.}
}
\value{
The Fréchet mean (an SPD matrix).
}
\description{
Computes the Fréchet mean (Karcher mean) of a list of Symmetric 
Positive-Definite (SPD) matrices.
This function can use either the Affine-Invariant Riemannian Metric (AIRM)
via the `shapes` package (if available and `metric="airm"`), or an iterative
algorithm based on the Log-Euclidean metric (`metric="logeuclidean"`).
}
